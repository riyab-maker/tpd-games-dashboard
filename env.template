# Environment variables for local preprocessing
# Copy this file to .env and fill in your database credentials
# NOTE: This is only needed for running preprocess_data.py locally
# The dashboard on Render does NOT need these credentials

DB_HOST=your_database_host
DB_PORT=3310
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
